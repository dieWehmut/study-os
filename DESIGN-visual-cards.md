# 图文笔记·第二形态：多元结构可视化信息图

第一形态（思维导图白板）已落地，见 `frontend/src/features/mindmap/`。
这份文件设计第二形态：把一段 wiki markdown 画成一张**有结构的图**，而不是一列方框长条。

需求出处：`prompt/0807.md` §图文笔记 第 2 条。
参考语料：`sample/distill/skill/knowledge-card/`（下称 skill）——它是同一件事的
prompt 版实现，本设计沿用它的**判定规则**，但换掉它的**执行方式**。

---

## 一、为什么换执行方式

0807:41 把 skill 的最大问题说得很准：

> 它似乎限制死了对于逻辑卡的排版长宽大小，导致遇到信息量略多的情况，并不会选择
> 扩展长宽保证结构的美观与可读性，而是选择滑档到 claude 系绘图最喜欢的**方框长条**
> 来极限展示完所有信息。……一定得能生成出怎样优雅的结构，就得生成怎样优雅的结构图。

这个滑档不是模型不听话，是**没有测量能力时的必然结果**。skill 里有近百行
「版面预算」算术（`文字宽 ≈ Σ每字前进宽 × font-size`，中文 1.0em、拉丁 0.55、
`i l j 1 . ,` 0.30），存在的唯一原因就是写 HTML 的模型量不了自己画的东西，
只能靠手算去猜。猜不准就只好压缩，压缩到最后就是方框长条。

程序能量。`MindMap.tsx:labelWidth` 已经在做同一件事的粗糙版。
把它升级成 skill 那张更准的表并抽出来共用，**画布高度就可以由内容算出来**，
于是「不锁尺寸」不再是一句要求模型自觉的话，而是一行 `h = Σ blockH`。

连带好处：排版变成可回归的断言。skill 的自查清单里那条
「导出 PNG 后自己 Read 那张图（必做，不看等于没做）」，是没有测量能力时的无奈补偿；
有了测量，「没有一块超出画布」「文字没压住引线」都能写成 vitest 断言。

---

## 二、管线

```
wiki markdown
   │ parseOutline()                          ← 已有，不动
   ▼ OutlineNode 树
   │ readBlocks()          lib/card-blocks.ts
   ▼ Block[] {title, lines[], children, fields[]}
   │ classifyStructure()   lib/card-structure.ts
   ▼ Structure（十类之一）
   │ layoutCard()          lib/card-layout.ts
   ▼ Frame {w, h, shapes[], texts[], links[]}
   ▼ <VisualCard/>         features/card/VisualCard.tsx
```

| 模块 | 职责 | 依赖 |
|---|---|---|
| `lib/text-metrics.ts` | 前进宽表 + `measure()` + `wrap()` | 无 |
| `lib/card-blocks.ts` | outline → 语义块 | `outline.ts` |
| `lib/card-structure.ts` | 十类判定 | `card-blocks.ts` |
| `lib/card-layout.ts` | 骨架 → 坐标 | `text-metrics`, `card-structure` |
| `features/card/VisualCard.tsx` | SVG 渲染，零逻辑 | `card-layout` |
| `features/card/exportPng.ts` | SVG → PNG | 无 |

与导图共享 `parseOutline`：同一份 markdown 出来的树，导图画成可折叠的枝丫，
卡片画成有几何的图。**两者都不经过模型**，所以同一份 wiki 每次画出来都一样
——0807:75「靠固定的程序就能实现文转 mindmap」，这条对卡片同样成立，
而且这正是它能用来预习的前提：结构每次都一样，才值得先看一遍。

### 一个块携带什么

`OutlineNode` 是给导图用的：一个标题加一串散文。卡片还需要知道
「这段散文里哪些是**带名字的值**」——表格的一行、`定义：mv` 这样的冒号句——
因为几何要拿它们去对齐成列。

`parseOutline` 已经把这件事做完了，只是编码成了散文：一个表格行进来时，
`body` 是 `["定义：mv", "例子：小球碰撞"]`，即 `表头：值`；
一个长列表项进来时，`title` 是主语、`body[0]` 是说明。
所以 `readBlocks` 只做一件事：把 `body` 每行按**首个**冒号再拆一次，
拆得开的进 `fields[]`（`{name, value}`），拆不开的留在 `lines[]`。

**`fields[]` 是名值对，不是定位网格。** 因为 `parseOutline` 会丢掉空单元格
（`outline.ts:327` 的注释：半填的表格是常态，`例子：` 后面空着读起来像漏答案），
所以第 3 行的第 2 个 field 未必和第 2 行的第 2 个 field 同列。
表格几何按**名字**归列，不按下标——名字本来就在每一行上，用它比用位置更可靠。

---

## 三、十类判定

`structures.md` §1.1 的派生律是这套判定能做小的原因：

```
                  ┌─ +箭头 +序号 ──→ 流程 ─ +闭合 ──→ 循环
并列（根结构）────┤
                  └─ +中心主题 ────→ 发散
```

所以十类不是十份代码。并列是根，流程/发散/循环是并列加开关，
真正需要独立几何的只有：表格、时间、层级、对比、矩阵、重合。

§1.3「边界判别硬规则」表面上是给人读的，但每一条都能从 markdown 机械判定：

| 硬规则（§1.3 原文判据） | 程序判据 |
|---|---|
| 表格 vs 矩阵：有没有坐标轴 | markdown 表格默认**表格**；恰 2 列 × 2 行、且行列名四个全部命中对立词表，才是矩阵 |
| 无轴四宫格 → 并列 | 同上，无轴一律并列 |
| 并列 vs 发散：中心有没有内容 | 父节点自己有没有 `body` 散文 |
| 循环 vs 流程：首尾接合 | 末项的标题或散文里出现首项标题，或命中 `/回到|重新|下一轮|周而复始|循环/` |
| 流程 vs 时间：要素自带时间戳 | `/\d{4}\s*年|\d+\s*月|\d+:\d{2}/` 命中率 ≥ 半数 |
| 层级 vs 流程：层级不加箭头，用量差编码 | 有序列表 + 动作动词 = 流程；量级词（一级/二级、大/中/小）= 层级 |
| 对比几乎恒为 2 项 | 恰 2 项且两个标题命中同一组对立词 |

对立词表是一张**写死的常量表**（高/低、强/弱、大/小、快/慢、难/易、正/反、
是/否、优/劣、前/后、内/外、急/缓、多/少），不是词性分析。
「对立形容词」这种说法程序判不了，一张十几对的表能判——
而且判错的代价已经被兜住：落回并列。

**判不出来就落回并列。** skill 自己写了「并列是根结构，任何并列骨架加箭头
就是合法流程骨架」，所以落回它永远是合法输出。这条保证这套东西**永远有图**，
不会因为判不准而拒绝出图——对一个用来降低启动成本的工具，
「有时候没有输出」比「有时候图选得一般」糟得多。

### 卡型（粒度）

skill §2 的卡型判据是「画完需要几个结构」，同样可数：

| 结构数 | 卡型 | 形态 |
|---|---|---|
| 0 | 金句卡 | 纯排版，句子即主图 |
| 1 | 单点卡 | 一图一结论 |
| 2–4 | 逻辑卡 | 多结构焊成一张图 |
| ≥5 | 地图卡 | 长幅，分区导航 |

skill 给地图卡加了「源文本 ≥2500 字」的体量闸门，理由是分区多 ≠ 地图卡。
本设计**不要这道闸门**：闸门存在是因为模型一次画不出 8 张标准卡，
而程序按块拼接没有上下文长度问题。0807:35 说的正是这件事
——地图卡应该是「多张标准卡片在统一框架下拼接」，而不是压缩版。

---

## 四、不锁尺寸

skill 的画布是常量 `1133 × 1511`，超出**静默裁切**。本设计没有这个常量。

```
cardHeight = padding×2 + Σ(blockHeight) + gap×(n−1)
cardWidth  = max(minWidth, 最宽块所需宽度)
```

`minWidth` 取 640——不是版面预算，是**下限**：比这更窄的卡片放不下两列，
所有骨架都会退化成一列，那就又是方框长条了。上限没有。

块高由内容算：一段文字的高 = `wrap()` 出来的行数 × 行高。
没有「装不下就缩字号」这一步——skill 自己也写了「到了下限还装不下 → 拆卡，别再缩」，
但它必须拆卡是因为画布是死的。画布活了，就长高。

**字号下限 18px 仍然保留**，它是可读性约束，不是版面约束。

---

## 五、页面落点

一个 `<VisualCard/>` 复用到三处已有 wiki 文本的地方，外加一个新页：

| 位置 | 形态 |
|---|---|
| 知识库 `WikiPanel` | 与「正文 / 导图」并列的第三个 tab |
| 阅读 `Reading` | 同上 |
| 融合 `Integrate` | 同上 |
| **新增「预习」页** | 按学科罗列卡组，点进去全屏 |

预习页是新的入口，对应 0807:73：

> 这 2 个东西，至少我需要它们的直接原因是为了**预习而非复习**。
> 提前预览这段信息的结构（chunk 化）或查看可视化信息图，
> 能指数级提升我在正式阅读/学习时的专注度/理解效率/记忆效率。

所以预习页的排序不是「最近编辑」，而是**「还没排进复习的排前面」**。

没有单独的「读过没有」字段，也不加一个——仓库已经有一处等价信号：
一个词条有没有 prompts，就是它有没有进过复习队列（`MEMORY.md`
「Has prompts IS is in the queue」）。没进队列的，就是还没正经学过的。
`db.ScheduledKnowledgeIDs` 一次查询就能给整页答案，用它，
不要再造第二个会漂移的真相源。

---

## 六、错误处理

| 情况 | 行为 |
|---|---|
| markdown 为空 | 不渲染卡片区，不报错 |
| 结构判不出 | 落回并列（见 §三） |
| 单块内容超长 | 卡片长高。没有裁切路径 |
| 导出 PNG 失败 | 保留按钮可按并说明原因，不静默失败 |

最后一条是仓库既有的 UI 铁律（见 `MEMORY.md`「一个控件不能在写失败时闩死」）：
成功才禁用，失败要留得住手并说清楚。

---

## 七、测试

全部在 vitest 里，不需要浏览器：

| 层 | 断言 |
|---|---|
| `text-metrics` | 中文/拉丁/上下标的前进宽；`wrap()` 不切断词 |
| `card-blocks` | 一份 markdown → 期望的块数与层级 |
| `card-structure` | §1.3 每条硬规则一个用例 + 判不出落回并列 |
| `card-layout` | **没有任何形状超出 `frame.w/h`**；块不重叠；长内容使卡片长高而非裁切 |
| `VisualCard` | 可访问名、tab 切换、导出失败留得住手 |

语料回归：`sample/distill` 的 72 份 markdown 全量跑一遍，
断言「零裁切、零重叠」。这条是照抄导图那次的经验——
单元用例和解析器出自同一套心智模型，会共享盲点；语料不会。
（`sample/` 是 gitignored，所以语料扫描是**一次性 harness，不进仓库**。）

---

## 八、分期

每期都能独立使用，不做「等下一期才有用」的中间层。

1. **`text-metrics` + 并列/流程/发散/循环**（一个引擎四个开关）→ WikiPanel 挂上 tab
2. **对比 / 层级 / 表格**（三个独立几何）
3. **时间 / 矩阵 / 重合**（补齐十类）
4. **预习页** + 三处复用铺开
5. **导出 PNG**
6. **地图卡**：块拼接（≥5 结构）

之后另起一轮：把 Practice 的错题从 `localStorage` 搬进后端
（`questions` / `question_attempts` / `error_causes` 三张表），
这是 `ROADMAP.md` 第 1 阶段，也是全库唯一一处假数据。
