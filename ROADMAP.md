# 总体规划：从「一堆工具」到「一套学习操作系统」

这份文件回答两个问题：**现在缺什么**，以及**按什么顺序补**。

它不重复 `prompt/0801.md`（对象定义与模块设想）、`prompt/0803_summarize.md`
和 `prompt/0807.md`（图文笔记与课程生成）。
那三份是需求；这份是把需求投影到当前代码库上得到的差集与执行序。

---

## 一、对象盘点：0801 的 13 个底层对象，现在有几个

schema 版本 7（`backend/db/db.go:19`），下表按 0801「三、首先要统一的底层对象」逐条对照。

| # | 对象 | 现状 | 落点 |
|---|---|---|---|
| 1 | 学科 Subject | **列，不是表** | `knowledge_items.subject` 等多张表上的 TEXT 列 |
| 2 | 原始材料 Source | ✅ | `sources` |
| 3 | 课程 Lesson | ❌ **完全没有** | — |
| 4 | 知识点 Knowledge Point | ✅ 主体有，关系没有 | `knowledge_items`；缺前置/易混/掌握状态 |
| 5 | 二级结论 Insight | 🟡 靠 `item_type` + tag 表达 | 知识库已有「二级结论/解题策略/易错信号」筛选 |
| 6 | 记忆项 Memory Item | ✅ | `prompts` + `review_states` |
| 7 | 题目 Question | ❌ **完全没有** | — |
| 8 | 作答 Attempt | 🟡 只有记忆项的作答 | `attempts.prompt_id NOT NULL`，无法记题目作答 |
| 9 | 错因 Error Cause | ❌ 只在浏览器里 | `frontend/src/lib/mistakes.ts` → `localStorage` |
| 10 | 任务 Task | ❌ | — |
| 11 | 学习会话 Study Session | ✅ 表在，写得少 | `study_sessions` |
| 12 | 答疑记录 Q&A | 🟡 存了对话，没存结构 | `chat_messages`；缺「原误解—正确模型—掌握证据」 |
| 13 | 系统想法 System Idea | ❌ | — |

另有一项 0801 列为「模块十」的基础设施**已经就位**：
`domain_events`（`schema.sql:92`，读写在 `store.go:695/710`）。统一事件层不需要从头设计。

## 二、最大的一处「假数据」

0801 设计原则第 ② 条是「一个对象，多种视图」——底层只能有一条记录。

做题工具（`frontend/src/pages/Practice.tsx`）违反了它：错题记录写进 `localStorage`，
后端看不见、连不上、也没法据此排复习。`lib/mistakes.ts` 里那套六类错因分类
（想不起来 / 看错题 / 算错 / 思路不对 / 没时间 / 还没想清楚）连同 `reviewFixes` 判定
都是真业务逻辑，但它作用在一份系统其余部分无法访问的存储上。

这正是「不要 mock 数据」要消除的东西，也是把 7、8、9 三个缺失对象一次性补齐的入口。

## 三、执行序

原则：**每一步都能独立使用**，不做「等下一步才有用」的中间层。

### 第 1 阶段：题目—作答—错因 落地到后端

1. `questions` 表：题干、学科、题型、来源、关联知识点
2. `question_attempts` 表：一题多做，记作答、用时、正误
3. `error_causes`：错因从 `mistakes.ts` 的常量升级成带学科维度的数据
   （0801：「以后你独立总结出的『地理六类错因』，就可以作为地理学科的专属分类体系」）
4. Practice 页改读后端；localStorage 只作为迁移来源，读一次后写入库

判据：错题能和知识点、记忆项 join 到一起；换设备后错题还在。

### 第 2 阶段：错因驱动复习

`mistakes.ts` 已经区分了「复习能修的」和「复习修不了的」。目前这个判断只用来显示文字。
接上后端后，`reviewFixes: true` 的错题应当能一键排进复习队列
——复用 `POST /api/knowledge/{id}/schedule` 这条已经打通的桥。

判据：做错一道题，其对应知识点当天进入队列。

### 第 3 阶段：课程 Lesson

0801 模块五的七阶段流水线是最大的一块。先只做能立刻用的部分：
课程对象 + 固定十段模板的骨架 + 与知识点/记忆项/题目的关联。
**不**先做 Agent 一次性生成整节课——0801 明确反对这种做法。

### 第 4 阶段：任务 Task 与系统想法 System Idea

放最后。它们是元系统，在前面几个对象产生足够真实记录之前，
做出来只会变成 0801 说的「高级形式的回避学习」。

## 四、逐科落实

共用底座先建，再逐科铺开——0801「风险五」明确反对一开始就处理全部学科。

铺开顺序按「差异化成本」从低到高：

| 顺序 | 学科 | 这一科独有的东西 |
|---|---|---|
| 1 | 英语 | 已有词库管线、词族、四种题型、发音。差的是错题与阅读回流 |
| 2 | 物理 | 受力图、运动过程、图像斜率与面积；错因偏「模型选择」 |
| 3 | 化学 | 考点/题型/易错点三类 insight；方程式排版（下标进位宽度是 1.0em） |
| 4 | 数学 | 函数图像、参数滑块、分步推导、错误步骤定位 |
| 5 | 地理 | 图层、区域对比、因果链、剖面图；错因自成六类 |
| 6 | 语文 | 得分点覆盖式批改；素材与表达模板 |

每一科的落地内容 = 共用底座 + 三样东西：
**该科的 insight 类型**、**该科的错因子分类**、**该科的图/交互组件**。

## 五、图文笔记：两件预习用的东西

`prompt/0807.md` 补的一章，上面几节都没覆盖。前提先摆清楚：**这两样是为预习，不是复习**
（0807:73）。工作记忆容量低，先看结构再读正文，专注度和理解效率才上得来。

### 5.1 两种形式，不要混成一个

| | 思维导图白板 | 多元结构信息图 |
|---|---|---|
| 结构 | 简单树状，**单向朝右** | 十类结构任选 |
| 编辑 | 必须可轻易编辑 | 不可编辑也无所谓 |
| 生成 | **固定程序**，markdown → 树 | 需要顶尖模型 |

第三行是硬约束。0807:75：「只要 wiki 本身是 markdown 格式的信息，那其实靠固定的程序
就能实现文转 mindmap」。`frontend/src/lib/mindmap.ts:20` 的 `markdownToMindMap`
已经是这个做法——确定性、离线、跑两遍形状一样。

### 5.2 思维导图：差集

`features/mindmap/MindMap.tsx` 已有折叠、`+N` 计数、键盘操作。对着 0807:13-16 逐条核：

| 要求 | 现状 |
|---|---|
| 可折叠/展开任意一级 | ✅ |
| 中间层级重新展开后不影响子层级状态 | ✅ `collapsed` 只增删被点的那个 id |
| **单向朝右** | ✅ 深度决定列（`x`），子树决定行（`y`） |
| 节点样式不要一个个方框 | ✅ 只有标签与其下划线；剩下的 `<rect>` 是透明点击区与面板底 |
| 节点可以是笔记、图片 | ✅ 正文挂在节点上（≡），配图从 `![](...)` 抽出（▣） |
| 可轻易编辑 | ❌ 只读 |

导图这一侧只剩「可轻易编辑」。但它不是接着往下做的那一件——**目前没有任何入口能改
wiki 的 markdown**（`/knowledge` 只有读、tag、schedule），而导图是从 markdown 现推的。
所以编辑节点之前，得先有「编辑 wiki」这件事本身，否则改完没有地方落。

### 5.3 结构信息图：distill skill 留什么、丢什么

`sample/distill/skill/knowledge-card` 蒸馏自小红书卡片。判卡逻辑是可复用的资产，
画布尺寸是要丢掉的部分。

**留下** `references/structures.md` 的选型决策。它不是一张平表，是两步判定：先数要素
个数选并列母题（§1.2），再看语义加装箭头/序号/中心/闭合（§1.1 派生律）。配套两张表
同样是实测资产——每格容量（§1.2b：圆环节点 4–6 字，纵向横条每行 20–35 字）和边界判别
（§1.3：表格 vs 矩阵看有没有坐标轴）。`sci-{geo,physics,math,chem}.md` 里已目检的学科
骨架直接对应 0807:57 的「可渲染学科图」。

**丢掉**固定画布。标准卡写死 1133×1511（`SKILL.md:237`），超出**静默裁切**
（`SKILL.md:160`），`--measure` 只量高不量宽（`SKILL.md:297`），也没有
`data-card-width` 这种旋钮。这就是 0807:41 那个「滑档到方框长条」的成因：装不下时
唯一出路是压缩，不是加宽。

地图卡已经证明加宽可行——`map-cards.md:40`「栏式画布宽 = 360 × 栏数」，5 栏即 1800。
把它从地图卡的特例升成通则：**先按结构算需要多宽，再定画布**，而不是反过来。

**改生成结构。** 地图卡不该由一段上下文一次生成（0807:35）：主 agent 定风格与尺寸，
子 agent 各生成一张标准卡，最后在地图卡框架下拼接。

### 5.4 落点

两样都挂在已有 wiki 上，不新开页面。`WikiPanel` 是 markdown 的所在，也就是它们的输入端。

## 六、明确不做

- 不做仪表盘，直到前面几个对象有真实、稳定的数据（0801 风险六）
- 不做「一次性生成整节课」的 Agent（0801 模块五开篇）
- 不给 HTTP server 加 `WriteTimeout`（会切断 SSE 与 120s 对话）
- 任何需要长期手工维护、却没有明显改善学习的功能，砍掉（0801 原则 ⑤）
