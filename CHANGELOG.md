# 更新日志

## v0.3.0（2026-09-21）

这一版把「装什么」定了下来：**只有两个 Windows 桌面版**——x64（Intel / AMD）与 arm64。
基于浏览器的 PWA 启动器整条路线被删掉（服务程序包、一键安装脚本、Service Worker、Web App 清单、
`/api/launcher/close`）。它原来承担的两件事——分发和更新——现在都归桌面版自己：
桌面版自带更新服务，检查发布清单、校验 SHA-256、解包暂存、写重启脚本，都由它完成。
旧的一键安装 `scripts/install-pwa.ps1` 不再存在，桌面版安装改用 `install.ps1`。

### 安装与更新

- 一键安装改为桌面版：`irm https://raw.githubusercontent.com/dieWehmut/study-os/main/install.ps1 | iex`。
  按当前机器架构自动选 x64 或 arm64，默认装到 `%LOCALAPPDATA%\StudyOS`，并在桌面生成图标。
- 桌面版自己完成更新，设置页与启动弹窗的「更新」入口不再依赖启动器。
  Windows 不允许覆盖正在运行的程序，所以更新通过 `restart.cmd` 完成：
  等旧进程退出 → 换上新版本 → 重新启动。
- 更新只对安装器建的目录生效（`<安装目录>\versions\<版本>\StudyOS.exe`）。
  便携版与开发版如实报告「不可自动更新」，不会去覆盖一个不属于它的目录。
- 更新包必须是桌面版：压缩包里没有 `StudyOS.exe` 就直接拒绝，
  免得一个残留的启动器样式安装包被当成桌面版装上。
- 发布清单改用不含 BOM 的 UTF-8 写入。此前在 Windows PowerShell 5.1 上打包会写出带 BOM 的
  `manifest.json`，Go 端解析直接失败；读取端现在两种都能吃下。
- 修正一键安装脚本：文档里那条不带参数的 `install.ps1` 此前会因为缺少必填的
  `-ManifestLocation` 而报错，现在默认跟随已发布清单。

### 从 v0.2.x 升级

旧版升不上来，需要重跑一次安装命令。两条旧路线都走不通，原因是各自的更新入口：

- 旧的 PWA 版只认 `study-os-pwa-windows-<架构>.zip` 这个安装包名，
  而这一版不再发布它，检查更新只会得到「最新版本暂未提供本机安装包」。
- 旧的桌面版没有可用的更新入口：更新挂在启动器上，桌面版启动时更新状态就是
  「启动器模式未启用」，应用不会自己升上来。

重跑 `install.ps1` 即可。它把新版本装进 `versions\<版本>\` 并切换 `current.json` 指针：
装到一半失败会回滚到原来的版本；如果安装目录下已经有 `data\`，
安装前会先做一份带校验的备份（保留最近 5 份）。

两处默认目录不同，升级前值得看一眼：

- 旧的 PWA 安装默认在 `桌面\学习系统`，它的学习数据就在同一个目录下的 `data\`。
- 程序文件与学习数据是分开的：桌面版把学习数据放在 `%APPDATA%\StudyOS\data`
  （除非 `STUDY_OS_DATA_DIR` 另有指定），程序文件才在安装目录里。
- 所以升级程序本身不会动你的学习数据；要延续 PWA 时期的进度，
  把 `桌面\学习系统\data\` 里的 `study.db`（以及同目录下的音频、备份）
  复制到 `%APPDATA%\StudyOS\data\` 即可，再打开桌面版就是原来的进度。

### 练习：六科按自己的方式追错题

- 每科有自己的处方：学习重点、该做的动作、需要的证据、下一步，语言随学科走。
- 确诊之后直接打开对应的板子，并保留可恢复的证据（schema v18 `evidence_json`）：
  语文按得分点逐条对照、数学按步核对推导、英语把从句从长难句里抬出来让主谓挨在一起、
  物理画受力图或切分运动阶段、化学配平并指出差在哪、地理一环一环写因果链。
- 数学几何板可控：已知条件画出图形，拖动顶点看变化，随时恢复初始图形。
- 错因从写死的六项变成可扩展的分类表（schema v16 `error_causes`）：
  可新增、可确认、可归档；确认过的才会出现在选择列表里，历史自由文本不会丢。
- 订正从「点一下」变成可追溯的作答证据（schema v15）：答案、用时、是否正确都留下，
  并且不抹掉你曾经做错这件事。
- 六科错题总览：按首要错因聚合，并给出该科的下一步动作。

### 课程

- 新增课程对象（schema v12–v14）：固定十段模板、版本化文档、乐观并发、来源指针。
- 课程可关联知识点与题目，双向可查。
- 课程内即时练习可作答并保留证据，重新打开课程能看到「已作答 N 次」与最近一次结果；
  课程练习证据与 FSRS 记忆卡分开，互不改动调度。

### 阅读

- 顶部「阅读」改成 Markdown 分栏工作区：左侧原文，右侧渲染；结构与正文工作流保留。
- 渲染结果里高亮词与多词表达，点开是紧凑的词条卡片，本地没有就按需生成。
- 内置两份英语语料索引：3913 条 Word Wiki 与 1603 条多词表达／语法家族。
- 英语文章工作区：生成、保存、阅读、导出 PDF；PDF 按阅读语义排版。

### 答疑

- 结构化答疑记录（schema v17 `qa_records`）：原理解、正确模型、掌握证据、未解决部分，
  可关联知识点／题目／课程；刷新与重启后仍在。
- 已在聊天里写过的问题与回答会预填前两项，不必重新抄一遍。
- 本阶段不自动声称「已经掌握」：掌握证据由你自己填写，状态显式选择。
- GitHub Pages 展示版复用同一套前端 API，在当前浏览器会话内模拟读写。

### 整合（思维导图）

- 导图从「一列方框」改成有几何结构的图：单向朝右，分支与挂靠节点齐平，深的分支不再跑出屏幕。
- 节点按标题实际宽度伸展，长标题完整可读；节点是标签加下划线，不再画方框。
- 表格也能生成导图，每一行成为一个可比较的分支；能读出并列、流程、循环还是发散。
- 节点可以就地改名，改动落回 wiki 原文；节点可挂笔记与图片。

### AI 与设置

- 厂商模型名可自由输入，服务商返回的模型作为建议值，仍可键入任意模型。
- 侧栏头像改成本地品牌块，不再请求 GitHub 头像；离线时外观稳定。
- 设置项改为单列堆叠，不再横向摊开。

### 界面

- 桌面端侧栏可完全隐藏，内容区立刻占用释放的空间；选择记在本地，刷新后恢复。
- 聊天输入框操作区对齐，评论标题不再重复显示。

### GitHub Pages 展示版

- 后端不再参与的静态展示版：内存适配器覆盖此前要后端的读写路径，路由与交互可点。
- 每个路由挂载独立的 Giscus 讨论区。

### 验证

- 追加路由级覆盖：`/api/update/apply` 走 HTTP 到落盘全链路、桌面构造必然带更新服务、
  发布矩阵测试在 CRLF 检出下同样成立。

## v0.2.2（2026-08-10）

### 练习：每一科按自己的方式追错题

选完错因之后，页面不再只给一句建议，而是直接打开该科该错因对应的板子，让你在上面把事情做一遍。全部离线，不调用模型。

- 物理：受力图把力逐个画上去再合成，缺哪个力当场说出来；也能把一段运动切成阶段，逐段补上漏掉的量。
- 化学：方程式两边各元素分别数一遍（含括号），不配平就指出差在哪；化学式按印刷体上下标排版。
- 数学：分步推导逐行核对，定位到第一行出错的地方，不必整题重做。
- 地理：因果链一环一环写下来，断在哪一环直接标出来。
- 语文：答案逐条对照得分点，漏掉的那条会被点名。

### 朗读

- 接入任意 OpenAI 风格的语音接口，可保存多个音色角色并随时切换，每个角色都能试听。
- 朗读从第一句就开始，不再等整段合成完；公式会被念全，不再念一半。

### 整合（思维导图）

- 导图改为单向朝右，每个分支与它挂靠的节点齐平，深的分支不再跑出屏幕底部。
- 节点按标题实际宽度伸展，长标题完整可读，不再截断成「副热带高气压带的…」。

### 知识库与复习

- 英语词条旁边列出同族词，可以直接跳过去。
- 二级结论按该学科真正用的说法归类。
- 记下的错题可以标为「已订正」，但不抹掉你曾经做错这件事。
- 未来一周每天要问多少张卡，提前看得到。

### 界面

- 全站改用霞鹜文楷，中文不再落到系统默认字体上。
- 侧栏顶部换成头像加名字的版式。

### 修复

- 一键安装脚本能正确处理 GitHub 实际返回的 sha256 校验文件（它按字节流下发，脚本此前当成文本读，校验必然失败）。
- 语音角色自己的接口地址和音频格式设置真正生效，此前被前端写死的默认值盖掉了。
- 下拉框显示选项名称而不是内部取值。
- 改用标准的 `mobile-web-app-capable` 声明独立窗口，去掉已废弃的 Apple 私有标签。

## v0.2.1（2026-08-09）

### 阅读（新页面）

- 把一份材料拆成能一次读完的「站点」：先看骨架，再一站一站读，读完一站再走下一站。全程离线，不经过模型。
- 每一站都能标「读完」或「没看懂」；没看懂的会汇成一张清单，一键带去答疑，问题已经替你写好。
- 大纲里直接看得出哪些站在你身后、哪些还卡着，不用回头数。
- 同一份材料可以切成脑图看，不用再粘一遍。
- 读到一半关掉也不丢：文档、读到哪、标记一起存着，下次首页会问你要不要接着读。
- 关掉的材料进书架，可以换回来，也可以不打开就丢掉。
- 读懂的一段可以直接「收进知识库」，并顺手排进复习队列。
- 朗读当前段落，并高亮正在读的那一行。

### 练习（错题）

- 记一笔：写下哪道题，选一个错因就存下了 —— 六类错因里，只有「想不起来」是再复习一遍能解决的，页面直接告诉你其余五类该怎么办。
- 错题记在数据库里，不再只活在这个浏览器；旧浏览器里的记录会自动搬过去一次。
- 「想不起来」的错题可以一键排进复习队列，题干变成卡片，当天就会问到你。
- 填错错因的行可以撤回。

### 记忆与复习

- 今天的队列还剩多少，一眼看得到，不再只有两个数字。
- 知识库里没有复习卡的条目会被标出来，一键补上 —— 在此之前，收进知识库的内容永远不会被问到。
- 看英说中一类卡片改为自评评分，题型不同、界面不同。

### 首页

- 首页接着你上次的进度：没读完的材料、卡住的地方、按学科统计和真实连续天数。

### AI 接入

- 六家厂商统一注册，每家都有能用的配置编辑器，不再只有 DeepSeek 能填。
- 内置高考核心词表，装完当天就能导入开练。

### 修复

- 所有对外请求都加了超时。`http.DefaultClient` 的超时是 0，也就是永远等下去 —— 这让文档里写着的三处降级（离线判分、回退本地发音、更新检查失败提示）实际上永远不会触发。
- 云端发音失败时真的回退到本地语音，而不是静默失灵。
- AI 判为「答对」的卡片不再被当成答错来排期。
- 启动器改为顺延到空闲端口并公布真实地址，桌面图标打开的是真的应用；空闲计时从启动开始算，不再从 1970 年算起。
- 一键安装脚本补齐校验：强制 https、sha256 校验、压缩包越界路径拦截、覆盖安装前自动备份学习数据。

## v0.2.0（2026-08-03）

### AI 接入

- DeepSeek 真实接入：日常模型 `deepseek-v4-flash`、推理模型 `deepseek-v4-pro`，支持思考模式与 JSON 输出。
- 设置页厂商卡片：显示各厂商状态、切换活跃服务商、测试连通性；可填写/清除 API Key、选择模型、修改接口地址，密钥只写不回显。
- 新增任务类型：词 Wiki、造句、自由文本批改、记忆点抽取、义项压缩；离线时全部有本地兜底。
- 云端发音（DashScope CosyVoice，带时间轴），未配置时回退 Windows SAPI。

### 记忆与英语

- 英语词库清洗管线：按等级/标签过滤、词形还原分组（词族）、批量生成词 Wiki。
- 四种检测题型：看英说中、看中说英、语境填空（四选一猜词）、造句（AI 他评）。
- 知识分组与词族浏览；学习记录跨重启持久保存。

### 学科体系

- 内置语文、数学、英语、物理、化学、地理六学科；首页一键切换，知识库与记忆队列按学科过滤，导入时可映射学科字段。

### 界面

- 全中文界面；移除荧光效果；统一绿色下拉与滚动条；PC 侧栏可拖拽拉伸；首页导航精简。

### 安装与更新

- PWA 启动器：一键安装脚本 + 桌面图标，双击自动启动后端并打开学习界面，关闭窗口即释放资源。
- 自动更新：内置 GitHub Release 检查与后台更新；设置页可手动检查更新。
- Wails 桌面版同步发布（x64 / ARM64）。
