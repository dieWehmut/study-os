# Copy this file to .env.local and fill in local-only values as needed.
# Supported initial values: mock, openai
AI_PROVIDER=mock

# OpenAI-compatible provider settings.
# Never commit a real API key.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=
